# chessbot media

Screenshots and GIFs referenced by the README on `main`. Kept on this orphan branch so
media never enters `main`'s history.

To keep this branch small, replace files by amending the single commit and force-pushing
(`git commit --amend && git push --force origin media`) rather than stacking commits.
